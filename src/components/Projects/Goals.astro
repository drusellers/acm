---
interface Props {
    goals: string[]
}
const { goals } = Astro.props
---

<div class="bg-lime-200 dark:bg-lime-900 rounded px-4 py-2">
    <div class="font-bold">Goals</div>
    {goals.map(g => {
        return <div>{g}</div>
    })}
</div>