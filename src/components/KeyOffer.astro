---
interface Props {
	item: string;
}
const { item } = Astro.props;
---
<div class="flex flex-col px-4 py-0 border-l border-blue-400 dark:border-blue-700">
    <div class="font-heading font-bold dark:text-dim-400">{item}</div>
    <div class="not-prose dark:text-dim-500"><slot /></div>
</div>